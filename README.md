# Hello World C++ Application

This is a simple "Hello, World!" C++ application.

## Building

To build this project, you'll need CMake:

```bash
mkdir build
cd build
cmake ..
make
```

## Running

After building, run the executable:

```bash
./hello_world
```